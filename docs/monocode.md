# Monocode Compatibility Notice

Monocode was renamed to ScratchASM.

Use [scratchasm.md](scratchasm.md) for the current language reference. The compiler still accepts legacy `.mono` files for compatibility, but new files should use `.sasm`.
